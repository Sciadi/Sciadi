# prints all keys in a dict

def print_keys(my_dict):
    for key in my_dict:
        print(key)

# prints all key:value pairs


def print_dict(my_dict):
    for key in my_dict.keys():
        print(f"{key} -> {my_dict[key]}")

