#include <string.h>
using namespace std;
extern const char* PATH_TO_DB;
extern const char COMMA;

int new_account(string, float);


int deposit_amount(string, float);



int withdraw_amount(string, float);
int balance(string);
int print_holder();
int close_account();
int modify_account();
